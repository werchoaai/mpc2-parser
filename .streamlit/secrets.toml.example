# Template for Streamlit secrets. Copy to secrets.toml locally,
# or paste into Streamlit Community Cloud: App → Settings → Secrets.
#
# The real secrets.toml is gitignored — never commit a real password.

password = "change-me"
